{
  "name": "bible-reading",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "tsc -b",
    "start": "npm run build && npm run start -w bible-reading-demo"
  },
  "devDependencies": {
    "typescript": "^6.0.0",
    "vite": "^7.3.1"
  }
}